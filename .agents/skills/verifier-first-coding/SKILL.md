---
name: verifier-first-coding
description: Implement behind tests so the loop can close without a babysitter. Use when code will change.
---

# Verifier-first

1. Name the check (test, typecheck, contract).
2. Make it fail for the right reason (only if write is on).
3. Smallest change that should pass.
4. Run the check. Stop when it passes.

If you cannot name a check, you do not have a spec yet.
