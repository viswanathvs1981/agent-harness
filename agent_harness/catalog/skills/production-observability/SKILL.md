---
name: production-observability
description: Instrument traces, drift, cost, and latency for AI in production. Use when operating agents for real users.
license: Apache-2.0
metadata:
  ng_skill: operating-in-production
  version: "0.1.0"
---

# Production observability

Log thread_id, node, tool names, eval scores, token usage, cost, latency. Alert on eval regression and cost spikes, not just HTTP 500s. Keep raw traces for error analysis. Redact secrets. Calibrate CI evals to blast radius.
